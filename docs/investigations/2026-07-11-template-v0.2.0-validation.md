# Template v0.2.0 validation — Slice 0.5

**Date:** 2026-07-11
**Slice:** 0.5 — Build and test the clean distributable foundation
**Branch:** `audit/foundation-repository` (this report only)
**Template branch:** `template/main`
**Status:** Implementation validation evidence

British English. Neutral role language. No personal emails, no-reply addresses, local paths, hostnames, secrets or AI authorship metadata.

---

## 1. Template branch and root commit

| Item | Value |
| ---- | ----- |
| Branch | `template/main` |
| Root commit | `27c8e19f8f5823c8cb177826be1b5b3622867038` |
| Parent commits | **None** — true orphan root |
| Commit message | `v0.2.0 create clean AI Project Foundation template` |
| Author / committer | `gromitski` with configured GitHub no-reply identity |
| AI co-author trailer | **None** |

Orphan verification:

```text
git rev-list --parents --max-count=1 template/main
→ 27c8e19f8f5823c8cb177826be1b5b3622867038   (single SHA, no parent)
```

---

## 2. Exact distributable files

The orphan root commit contains exactly five tracked files:

```text
README.md
START.md
.gitignore
memory/agreements.md
.cursor/rules/00-project-foundation.mdc
```

No empty directories. No `docs/`, `context/`, `evidence/`, `specialist/`, `work/`, `memory/decisions/`, foundation specification, investigations, package files, CI or generators.

---

## 3. Initialisation dry-run method

1. Created `template/main` as an orphan branch in an isolated Git worktree (`../ai-project-foundation-template-worktree`).
2. Pushed `template/main` to `origin`.
3. Created a detached worktree clone from `template/main` for dry run (`../ai-project-foundation-dryrun`).
4. Replaced `START.md` with the supplied fictional household-consumables intent.
5. Executed the README initialisation instruction manually (simulated AI): created `memory/intent.md`, `memory/now.md`, `evidence/origin/2026-07-11-origin.md`; replaced `START.md` with pointer; updated README with project name and one-line description only.
6. Did **not** commit dry-run output to `template/main`.
7. Removed both temporary worktrees after validation. Original audit working tree unchanged.

---

## 4. Fictional-project results

**Project name used:** Household Consumable Tracker

### Files present after initialisation

```text
README.md
START.md
.gitignore
memory/agreements.md
memory/intent.md
memory/now.md
evidence/origin/2026-07-11-origin.md
.cursor/rules/00-project-foundation.mdc
```

### Not created (confirmed)

- `context/`
- `specialist/`
- `work/`
- `memory/decisions/`
- architecture, deployment or roadmap documentation
- framework or package files

### Memory quality review

| Check | Result |
| ----- | ------ |
| Honest uncertainty (interface form undecided) | Pass |
| No premature stack choice | Pass |
| No fictional infrastructure | Pass |
| One practical first slice (CSV manual spike) | Pass |
| No PII | Pass |
| No local paths | Pass |
| No system specifications | Pass |
| No secrets | Pass |
| No AI authorship metadata | Pass |
| README not duplicating current state | Pass — name, one line, memory links only |
| START.md remains as pointer | Pass |

**First useful slice could begin immediately:** Yes — next action is a one-hour CSV manual-tracking spike.

### Friction observed

- README initialisation block is long because it embeds output contracts; acceptable trade-off for idempotent, self-contained initialisation without a separate manual.
- User must copy one Markdown block — no separate prompt file (by design; keeps day-zero file count at five).

---

## 5. Measurements

### Day-zero template (`template/main`)

| Metric | Value |
| ------ | ----- |
| Tracked files | 5 |
| Markdown / MDC word count | 1,261 |
| Total bytes (all tracked) | 9,238 |
| Always-applied Cursor rules | 1 |
| Cursor rule body words (excl. front matter) | 115 |
| `memory/agreements.md` words | 611 |
| `memory/agreements.md` lines | 84 |

### Post-initialisation dry run (fictional project, uncommitted)

| Metric | Value |
| ------ | ----- |
| Total files (excl. `.git`) | 8 |
| Canonical memory words (`intent` + `now` + `agreements`) | 885 |
| Initialisation steps | Write intent → run prompt → review → begin slice (~4 human steps) |

---

## 6. Comparison with old baseline (Slice 0.1)

| Metric | Old foundation | New template v0.2.0 | Verdict |
| ------ | -------------- | --------------------- | ------- |
| Prose / MD words (default load) | ~9,590 | 885 canonical after init; 1,261 day-zero (incl. README prompt) | **Material improvement** for canonical memory |
| Files in generated context | 16 | 3 canonical (+ agreements pre-init) | **Material improvement** |
| Generated context size | ~47 KB dump | No generator yet; canonical spine ~6 KB post-init | **Improved**; generator deferred |
| Always-applied Cursor rules | 6 | 1 | **Material improvement** |
| Tier / placeholder setup | Required | **None** | **Eliminated** |
| Empty operational shells | Many | **None** | **Eliminated** |
| Adoption model | Trim/delete | Initialise from intent | **Inverted as designed** |

---

## 7. Leakage-scan results

Scanned all tracked content on `template/main` for foundation-maintainer material, PII and sensitive patterns.

| Pattern | Result |
| ------- | ------ |
| `FOUNDATION-SPEC` | Not found |
| `foundation-repository` | Not found |
| `BlooPlax` | Not found |
| `Wye Trains` | Not found |
| `Stu` | Not found |
| Personal email patterns | Not found |
| `/Users/`, `/home/` | Not found |
| GitHub no-reply email in content | Not found |
| Secret/token patterns | Not found |
| Audit/investigation paths | Not found |

**Policy-only matches (acceptable):** `memory/agreements.md` mentions `cursoragent`, `Co-authored-by`, `Cursor` and `ChatGPT` only as **prohibitions** in Git authority rules — not as metadata or attribution.

**Approved handle:** `gromitski` appears in agreements where Git identity is required.

**Verdict:** **Pass** — no foundation autobiography or sensitive leakage.

---

## 8. Privacy and commit-metadata results

| Check | Result |
| ----- | ------ |
| Orphan history | No inherited personal email or AI trailers |
| Root commit author | `gromitski` |
| Root commit email | GitHub no-reply (metadata only) |
| `Co-authored-by` on root commit | Absent |
| PII in template file bodies | Absent |
| Local-system information in template | Absent |
| `.obsidian/` in template | Absent; ignored in `.gitignore` |

---

## 9. Orphan-history verification

| Command | Output |
| ------- | ------ |
| `git rev-list --parents --max-count=1 template/main` | Single SHA, no parent |
| `git merge-base template/main audit/foundation-repository` | No common ancestor (orphan) |
| `git merge-base template/main main` | No common ancestor (orphan) |

Template branch is isolated from maintainer design history and legacy `main`.

---

## 10. Problems found and corrected

| Issue | Resolution |
| ----- | ---------- |
| `.obsidian/` untracked locally on audit branch | Already ignored from Slice 0.4; not present on template |
| Slice 0.4 review: one Cursor rule not three | Implemented single `00-project-foundation.mdc` |
| Slice 0.4 review: no `context/` day-zero | Not created |
| Slice 0.4 review: README must not duplicate current state | Dry-run README limited to name + one line + links |
| Slice 0.4 review: START.md pointer after init | Implemented exactly |
| Temporary worktrees | Removed after push and validation |

No template content required correction after commit.

---

## 11. Remaining issues

1. **Generated context not implemented** — remote multi-AI handoff still relies on reading three canonical files directly.
2. **Initialisation is manual prompt execution** — no script idempotency enforcement yet; depends on AI following README contract.
3. **Commit policy for future generated views** — unresolved A/B test from Slice 0.4.
4. **Stack-specific `.gitignore` fragments** — deferred until stack trigger.
5. **Legacy `main` and audit branch** — still contain pre-v1.0 history; template orphan does not fix maintainer-repo history (by design).
6. **Real-project validation** — Wye Trains and BlooPlax migration not yet attempted on live repositories.

---

## 12. Recommendation for next slice

**Slice 0.6 — Selective generated context and handoff experiment**

1. Implement minimal generator producing a manifest + selective snapshot from canonical memory only.
2. Run commit-vs-on-demand policy A/B using the fictional project and one real small experiment.
3. Add freshness markers and stale-view detection.
4. Keep generator off `template/main` until proven; add via separate implementation slice or tagged template release.

Alternative parallel track: **migration dry-run** mapping BlooPlax living docs onto the new spine (investigation only, on audit branch).

---

## Document control

| Item | Value |
| ---- | ----- |
| Report role | Evidence that template v0.2.0 meets Slice 0.5 acceptance |
| Template pushed | `origin/template/main` |
| Audit branch change | This report only |
